---
name: dose-debt
description: Harvest `dose:` comments into a debt ledger.
---

Grep for `dose:` comment markers. One row per hit.

Format: `<file>:<line> — <what>. ceiling: <limit>. upgrade: <trigger>.`
End with `<N> markers, <M> with no trigger.`
