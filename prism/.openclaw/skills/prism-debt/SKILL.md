---
name: prism-debt
description: Harvest `prism:` comments into a debt ledger.
---

Grep for `prism:` comment markers. One row per hit.

Format: `<file>:<line> — <what>. ceiling: <limit>. upgrade: <trigger>.`
End with `<N> markers, <M> with no trigger.`
