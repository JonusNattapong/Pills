---
name: prism-create
description: Create a custom Prism pill with its own ladder, rules, and skill.
---

# Prism Create

Create a custom pill. Define its identity, ladder, and rules.
Run `node scripts/generate-pill.js --name <name> --emoji <emoji> --color <color> --focus "<focus>"` to generate files.
